# 派生产物：由 scripts/build_index.py 从本体重新生成
build/

# 仿真运行结果：由 scripts/simulate.py --output 生成，README 里已归为派生文件。
# 场景（simulation/scenarios/）是输入、要进版本库；跑出来的结果是输出、不进。
# 留着会让 daily_refresh.py 的 require_git_clean 前置检查拦住下一次自动扩充。
simulation/runs/

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# playwright MCP 的页面快照与 console 日志，落在当前工作目录。
# 必须忽略：它是检索过程的副产物，不是知识；留着会让下次
# daily_refresh.py 的前置检查因"工作区不干净"直接拦住。
.playwright-mcp/

# 临时与备份
*.bak
*.tmpbak
*.tmp
.DS_Store
Thumbs.db
logs/
